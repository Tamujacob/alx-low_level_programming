#include <stdio.h>

/**
 * main - prints " A program thats prints a line with puts function"
 * Return: Always 0 (Succes)
 */
int main(void)

{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
