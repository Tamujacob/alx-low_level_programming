#include <stdio.h>
/**
 * main - Prints alphabetic letters in lower case
 * Return: ALways (Succes)
 */

int main(void)
{
	char c;
	for (c = 'a'; c <= 'z'; c++)

	putchar(c);
	putchar('\n')
		return (0);
}
